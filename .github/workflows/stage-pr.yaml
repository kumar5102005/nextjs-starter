name: Deploy on comment
on:
    issue_comment: 
        types: created

jobs:
    deploy-to-new-env:
        runs-on: ubuntu-latest
        steps:
            - run: echo '${{ toJson(github.event) }}'
            - run: echo '${{ toJson(github.ref) }}'